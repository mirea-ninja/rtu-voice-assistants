slots:
    type:
        source: $When
        type: DateTimeType

root:
    ($Amount)? (пар|уроков|занятий)? (в|во)? ($When)?;

$Amount:
    %lemma
    какие
    сколько
    количество
    пары
    уроки
    занятия

$When:
    $DateTimeType